const express = require("express");
const pool = require("../config/db");
const router = express.Router();

//need a router.get to obtain a list of all the requests associated with the current user 

//need a router.post to create a friend request between the current user and another user in the system 

//need a router.delete to remove a single friend request associated with the current user 


module.exports = router;
